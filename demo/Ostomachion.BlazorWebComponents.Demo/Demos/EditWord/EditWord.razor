@inherits WebComponentBase<EditWord>

@{
    HostAttributes["tabindex"] = 0;
}

<form>
    <input hidden="@(!Editing)" required @bind="Value" @ref=_input @onblur="Blur" />
</form>
<span hidden="@Editing" @onclick="Click">@Value</span>
